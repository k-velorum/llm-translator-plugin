# 手動スモークテストチェックリスト

自動テストで守れない Chrome API / DOM / 外部 API 依存の挙動を検証する共通手順。
各フェーズの作業単位ごとに**関連項目**を、フェーズ6では**全項目**を実施する。

前提: `chrome://extensions` でデベロッパーモード有効、本リポジトリを「パッケージ化されていない
拡張機能を読み込む」でロード。コード変更後は必ず拡張をリロードし、**テスト対象タブも再読み込み**する
（古い content script が残るため）。

## 0. 起動・基本

- [ ] 拡張ロード時に service worker のエラーが出ない（chrome://extensions → 「エラー」なし）
- [ ] popup が開き、コンソールにエラーがない
- [ ] オプション保存 → popup を閉じて再オープン → 設定が復元される

## 1. 選択テキスト翻訳

- [ ] 任意ページでテキスト選択 → 右クリック → コンテキストメニューから翻訳 → ポップアップ表示
- [ ] キーボードショートカット（Ctrl+Shift+T / Cmd+Shift+T）で同様に動く
- [ ] ストリーミング対応プロバイダーで逐次表示される
- [ ] ポップアップ外クリックで閉じる。**20回開閉してもリスナーが増えない**
      （DevTools console で `getEventListeners(document)` の件数確認 — フェーズ5）
- [ ] 翻訳中に閉じた場合にエラーが出ない（キャンセル経路）
- [ ] iframe 内のテキスト選択でも動く（all_frames 検証）

## 2. ページ翻訳

- [ ] コンテキストメニュー「ページを翻訳」で全文翻訳が進行し、進捗表示が出る
- [ ] キャンセルが効く
- [ ] 長いページ（チャンク分割が複数回走る規模）で完走する
- [ ] 429 等の一時エラー時にリトライされる（確認できる場合）

## 3. X (Twitter)

- [ ] タイムラインのツイートに翻訳ボタンが注入される
- [ ] 無限スクロールで新しく現れたツイートにもボタンが付く（MutationObserver）
- [ ] 翻訳実行 → 結果がツイート下に表示。同じツイートの再翻訳はキャッシュから即時表示
- [ ] 設定でTwitter翻訳を OFF → ボタンが出ない / ON に戻す → 再び出る（Observer 再開）

## 4. YouTube

- [ ] コメントに翻訳ボタンが注入され、翻訳が表示される
- [ ] 設定 OFF/ON の切り替えが効く

## 5. 画像翻訳（LM Studio）

- [ ] 画像を右クリック → 翻訳 → 結果表示（LM Studio + vision モデル環境がある場合）

## 6. プロバイダーマトリクス

各プロバイダーで「設定 → 検証 → モデル一覧 → 翻訳テスト（popup の Test）→ 選択翻訳」を確認:

| プロバイダー | キー検証 | モデル一覧 | 翻訳 | ストリーミング | 備考 |
|---|---|---|---|---|---|
| OpenRouter | 未確認 (2026-06-12) | unit で request shape 固定 | 未確認 (2026-06-12) | 未対応 | |
| Gemini | 未確認 (2026-06-12) | unit で request shape 固定 | 未確認 (2026-06-12) | 未対応 | |
| Cerebras | 未確認 (2026-06-12) | unit で private/public fallback 固定 | 未確認 (2026-06-12) | 未確認 (2026-06-12) | |
| Z-AI | — | 静的モデル | 未確認 (2026-06-12) | 未対応 | |
| Ollama | — | unit で request server 優先順を固定 | 未確認 (2026-06-12) | — | ローカルサーバ要 |
| LM Studio | — | unit で Tailscale URL を固定 / 実機 popup で保存済み server 復元 | PASS (popup Test / 選択翻訳 / ページ翻訳, 2026-06-12) | 未確認 (2026-06-12) | ローカルサーバ要。**Tailscale 経由 URL でも確認**（d437a77 回帰防止） |
| Chrome Gemini Nano | —（キー不要） | — | 未確認 (2026-06-12) | 未対応 | offscreen 経由。Nano 有効環境要 |

実機が用意できない項目は「未確認」と日付付きで記録し、用意でき次第消化する。

## 7. エラー経路

- [ ] 不正な API キーで検証 → 分かりやすいエラー表示（popup・選択翻訳の両方）
- [ ] ネットワーク遮断状態で翻訳 → エラー表示がクラッシュせず出る
- [ ] **エラー表示・console・保存ログのどこにも API キーが平文で出ない**（フェーズ2以降必須）

## 8. アップグレード互換（フェーズ4・6で必須）

- [ ] リファクタ前バージョンで全プロバイダーの設定を保存したプロファイルに新版をロードし、
      設定（キー・モデル・カスタムプロンプト・機能 ON/OFF）が全て引き継がれる
- [ ] 拡張リロード直後、リロード前から開いていたタブで操作してもコンソールがエラーで溢れない

## 実施記録

- 2026-06-12: 自動検証のみ実施。`bun run lint` error 0、`bun run test` 37 tests pass、`manifest.json` parse OK。
- 2026-06-12: フェーズ7後の自動検証を実施。`bun run lint` error 0 / warning 29、`bun run test`
  10 files / 45 tests pass、`manifest.json` parse OK。
- 2026-06-12: 通常 Chrome の既存プロファイルで拡張 popup を開き、設定 tab で保存済み LM Studio 設定
  （server `http://100.115.98.13:1234`）が復元されることを確認。Test tab で LM Studio を選択し、
  既定テスト文の日本語翻訳が返ることを確認。
- 2026-06-12: `127.0.0.1` の一時ページで選択翻訳を確認。main frame と iframe 内テキストの両方で
  コンテキストメニュー `LLM翻訳` が表示され、翻訳 popup に結果が表示された。
- 2026-06-12: 同一一時ページで `LLMページ全体翻訳` を確認。進捗 UI が表示され、main frame の本文が
  日本語訳へ置換された。
- 2026-06-12: X home でツイート 8件に翻訳ボタン 8件が注入され、スクロール後もツイート 10件 /
  ボタン 10件になったことを確認。1件翻訳し、ツイート下に日本語訳が表示された。
- 2026-06-12: YouTube コメント欄でコメント 20件に翻訳ボタン 20件が注入され、1件翻訳してコメント下に
  日本語訳が表示されたことを確認。音声が出る動画タブは検証後に閉じた。
- 2026-06-12: popup の Feature tab で Twitter / YouTube 翻訳を OFF にして保存後、X / YouTube の
  既存ボタン・翻訳表示が 0 件になることを確認。最後に両方 ON に戻して保存。
- 2026-06-12: 未確認: service worker の `chrome://extensions` エラー表示、ショートカット、
  streaming 逐次表示、20回開閉時の listener 件数、キャンセル経路、長いページ、429 retry、
  画像翻訳、LM Studio 以外の実 API 疎通、不正キー/ネットワーク遮断/APIキーマスク、アップグレード互換。
